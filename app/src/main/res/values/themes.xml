<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.AboutYou" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/theme_dark</item>
        <item name="colorPrimaryVariant">@color/theme_dark</item>
        <item name="colorOnPrimary">@color/theme_light</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/theme_dark</item>
        <item name="colorSecondaryVariant">@color/theme_dark</item>
        <item name="colorOnSecondary">@color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.AboutYou.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.AboutYou.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.AboutYou.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="AppTheme.Picker" parent="Theme.AppCompat.Light.NoActionBar"
        >
        <item name="android:textColorHighlight">@color/white</item>
        <item name="colorControlNormal">@color/theme_dark</item>
        <item name="android:textColorPrimary">@color/white</item>
        <item name="android:textSize">30dp</item>
    </style>
</resources>